<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
      Reads which apps were last opened anywhere on the device, so the default
      screen can show genuine recents rather than only the launches made from
      here. This is special access, not a runtime permission: declaring it grants
      nothing and shows no prompt, and the user must switch it on by hand under
      Settings → Apps → Special app access → Usage access. Without it the
      launcher still works and falls back to its own launch history. Nothing read
      here leaves the device. It remains the app's only permission of any kind.
    -->
    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <!--
      Package visibility for Android 11+. This is the narrow, launcher-appropriate
      declaration: it makes exactly the apps a home screen must show visible to us,
      and nothing else. QUERY_ALL_PACKAGES is deliberately not requested.
    -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
        <!--
          Home apps, which the filter above does not cover: a launcher exposes
          MAIN/HOME and deliberately not MAIN/LAUNCHER, so without this the app
          currently holding the Home role is invisible to us and any attempt to
          resolve it answers with the only HOME activity we can see — our own.
        -->
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.HOME" />
        </intent>
    </queries>

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode|navigation|density|fontScale|locale|layoutDirection"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize|stateVisible"
        android:stateNotNeeded="true"
        android:clearTaskOnLaunch="true"
        android:resizeableActivity="true"
        android:exported="true">

        <!--
          Keeps the app launchable from the app drawer, Android Settings and
          `adb shell am start`, which matters while it is not the default home app.
        -->
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>

        <!-- Lets the user select this app as the device Home/Launcher. -->
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.HOME" />
            <category android:name="android.intent.category.DEFAULT" />
        </intent-filter>
      </activity>

      <!--
        Device administration, held for the single force-lock policy that makes
        double tap to lock possible. Exported and permission-guarded exactly as
        the platform requires: only the system may bind to it.
      -->
      <receiver
          android:name=".launcher.LockAdminReceiver"
          android:permission="android.permission.BIND_DEVICE_ADMIN"
          android:exported="true">
        <meta-data
            android:name="android.app.device_admin"
            android:resource="@xml/device_admin" />
        <intent-filter>
            <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
        </intent-filter>
      </receiver>
    </application>
</manifest>
